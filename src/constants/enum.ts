export enum UserVerifyStatus {
  Unverifyed = '0',
  Verifyed = '1'
}

export enum TokenTypes {
  AccessToken,
  RefreshToken,
  ForgotPasswordToken,
  EmailVerifyToken
}

export enum Roles {
  Admin = 'admin',
  Customer = 'customer'
}
